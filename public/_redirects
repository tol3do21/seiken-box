# Cloudflare Pages — Reglas de enrutamiento
# No se necesita fallback SPA porque el sitio es HTML estático.
# Forzamos que Cloudflare Pages sirva el CMS Sveltia en la ruta admin sin usar wildcard para evitar loop
/admin /admin/index.html 200
/admin/ /admin/index.html 200
